// if you want to add key in localStorage
// please add key or key's prefix here to prevent key conflict
// prefix should end with '/'

export const SETTINGS_KEY = 'settings';
export const REQUEST_CACHE_PREFIX = 'cache/request/';
